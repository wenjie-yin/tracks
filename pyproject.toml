[project]
version = "0.0.1"
name = "tracks"
dependencies = [
    "langchain",
    "langchain_google_vertexai",
    "langgraph",
    "pydantic",
    "pydantic-tfl-api",
]

[tool.isort]
profile = "black"
